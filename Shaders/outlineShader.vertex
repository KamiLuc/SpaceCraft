#version 330

layout(location = 0) in vec3 pos;
layout(location = 2) in vec3 norm;

uniform mat4 model;
uniform mat4 projection;
uniform mat4 view;

void main()
{
    float outlining = 2.08;
	gl_Position = projection * view * model * vec4(pos + norm * outlining, 1.0f);
}